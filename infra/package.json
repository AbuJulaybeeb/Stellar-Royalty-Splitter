{
  "name": "stellar-royalty-splitter-infra",
  "version": "0.1.0",
  "private": true,
  "description": "Operational tooling: backup manager (#937) and canary controller (#936). No dependencies of its own; SQLite access reuses backend/node_modules.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "node --check backup-manager.js && node --check canary-controller.js && bash -n recovery-test.sh",
    "test": "node --test tests/*.test.js"
  }
}
